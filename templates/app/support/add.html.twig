{% extends 'app.html.twig' %}

{% block title %}Questions/Demandes{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            {{ form_start(form_support_ticket) }}
            {{ form_errors(form_support_ticket) }}
            <h2 class="p-2"> Informations importantes :</h2>
            <div class="row">
                <div class="col-12 mt-2">
                    {{ form_row(form_support_ticket.title) }}
                </div>
                <div class="col-12 mt-2">
                    {{ form_row(form_support_ticket.type) }}
                </div>
                <div class="col-12 mt-2">
                    {{ form_row(form_support_ticket.content) }}
                </div>
            </div>
            <div class="p-2" align="center">
                <input type="submit" value="Créer la demande" class="btn btn-primary">
            </div>


            {{ form_end(form_support_ticket) }}
        </div>
    </div>

{% endblock %}
